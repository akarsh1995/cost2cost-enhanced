steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  args:
  - gcloud
  - functions
  - deploy
  - get_pricelist_json
  - --gen2
  - --region=us-central1
  - --set-env-vars=PROD=True
  - --source=./backend
  - --allow-unauthenticated
  - --trigger-http
  - --runtime=python310
